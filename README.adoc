= Equivalence Rules: Filesystem
:toc:
:icons: font

Cross-OS filesystem path equivalency rules (Linux, Windows, macOS, BSD) for the link:https://github.com/AMDphreak/equivalence-engine[Equivalence Engine].

== Ecosystem

* link:https://github.com/AMDphreak/equivalence-engine[Equivalence Engine] — The core binary that resolves and applies these rules.
* link:https://github.com/AMDphreak/equivalence-engine-action[Equivalence Engine Action] — Modular GitHub Action to automate code adaptation in CI pipelines.
* link:https://github.com/AMDphreak/equivalence-rules-code[Equivalence Rules: Code] — Rules for migrating source code between library versions.
* link:https://github.com/AMDphreak/equivalence-rules-filesystem[Equivalence Rules: Filesystem] — This repository.

== Overview

This repository is a **Content Repository** for the `filesystem` domain. It provides abstract "intents" (e.g., `lib_dir`, `config_dir`) and maps them to literal paths across different operating systems and distributions.

== Directory Structure

[source]
----
rules/
├── linux/              <-- OS Family
│   └── ubuntu/         <-- Distribution
│       └── 22.04/      <-- Version
├── windows/
└── mac/
----

== Usage with Equivalence Engine

To resolve a filesystem intent across platforms:

[source,bash]
----
equivalence-engine --domain filesystem \
  --rules-repo https://github.com/AMDphreak/equivalence-rules-filesystem \
  --to linux/ubuntu/22.04 lib_dir
----

== Contribution

Add new path mappings to the `rules/<os>/<distro>/<version>/` folders. Use the intent name as the filename (e.g., `rules/linux/ubuntu/lib_dir.sdl`).
